package Demo;

public class FuncString {

	public static void main(String[] args) {
		// Formatar String:
		// toLowerCase() Transformas para minusculo
		// toUpperCase() Transformar para maiusculo
		// trim() Remover espaços

		// Recortar String:
		// substring(informando inicio)
		// substring (informando inicio e fim)

		// Substituir caracteres ou substring em string:
		// Replace (char, char)
		// Replace (string, string)

		// Buscar posição em alguma parte do string:
		// IndexOf, LastIndexOf

		// Função Split:
		// str.Split(" ")Recorta String com base em separador que informar.

	}

}
